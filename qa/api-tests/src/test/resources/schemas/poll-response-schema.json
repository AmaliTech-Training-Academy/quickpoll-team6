{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Poll Response Schema",
  "description": "JSON schema for validating poll response structure based on OpenAPI PollResponse",
  "type": "object",
  "required": ["id", "question", "creatorEmail", "creatorName", "status", "maxSelections", "expiresAt", "createdAt", "totalVotes", "invitedDepartments", "options"],
  "properties": {
    "id": {
      "type": "integer",
      "description": "Poll identifier"
    },
    "question": {
      "type": "string",
      "maxLength": 500,
      "description": "The poll question"
    },
    "description": {
      "type": ["string", "null"],
      "description": "Poll description (optional)"
    },
    "creatorEmail": {
      "type": "string",
      "description": "Email of the poll creator"
    },
    "creatorName": {
      "type": "string",
      "minLength": 1,
      "description": "Name of the poll creator"
    },
    "status": {
      "type": "string",
      "description": "Poll status (e.g., ACTIVE, CLOSED)"
    },
    "maxSelections": {
      "type": "integer",
      "minimum": 1,
      "description": "Maximum number of options that can be selected"
    },
    "expiresAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 datetime when poll expires"
    },
    "createdAt": {
      "type": "string",
      "format": "date-time",
      "description": "ISO 8601 datetime when poll was created"
    },
    "totalVotes": {
      "type": "integer",
      "minimum": 0,
      "description": "Total number of votes across all options"
    },
    "invitedDepartments": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of invited department names"
    },
    "options": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["id", "text", "voteCount", "percentage"],
        "properties": {
          "id": {
            "type": "integer",
            "description": "Option identifier"
          },
          "text": {
            "type": "string",
            "minLength": 1,
            "description": "Option text"
          },
          "voteCount": {
            "type": "integer",
            "minimum": 0,
            "description": "Number of votes for this option"
          },
          "percentage": {
            "type": "number",
            "minimum": 0,
            "maximum": 100,
            "description": "Percentage of total votes"
          }
        },
        "additionalProperties": false
      },
      "description": "List of poll options with vote counts and percentages"
    }
  },
  "additionalProperties": false
}
