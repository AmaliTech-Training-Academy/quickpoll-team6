{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Vote Response Schema",
  "description": "JSON schema for validating vote cast response structure (POST /votes/polls/{pollId})",
  "type": "object",
  "required": ["success", "message"],
  "properties": {
    "success": {
      "type": "boolean",
      "description": "Whether the vote was successfully cast"
    },
    "message": {
      "type": "string",
      "minLength": 1,
      "description": "Response message describing the result"
    }
  },
  "additionalProperties": false
}
